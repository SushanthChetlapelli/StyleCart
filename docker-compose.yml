services:
  mongodb:
    image: mongo:7
    container_name: stylecart-mongodb
    ports:
      - "27017:27017"
    volumes:
      - stylecart_mongo_data:/data/db
    restart: unless-stopped

volumes:
  stylecart_mongo_data:
